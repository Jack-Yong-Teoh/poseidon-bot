version: "3.8"

services:
  discord-bot:
    build: .
    container_name: server_monitor_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./scripts:/app/scripts
    pid: "host"
